%h1 Listing attempts

%table
  %tr
    %th
    %th
    %th

  - @attempts.each do |attempt|
    %tr
      %td= link_to 'Show', attempt
      %td= link_to 'Edit', edit_attempt_path(attempt)
      %td= link_to 'Destroy', attempt, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Attempt', new_attempt_path
